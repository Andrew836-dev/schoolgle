{{> navTwo }}
<div class="container">

    <h3 class="collegeo"><span class="account-name">USERNAME</span>'s Schoolgle List</h3>
    <button type="button" id="printBtn" class="right hide-on-med-and-down"
        onclick="printJS({printable: 'list', type: 'html', ignoreElements: ['btnIgnore'], header: 'SchoolgleList'})">
        <span class="material-icons">
            print
        </span>
    </button>
</div>
<div class="container" id="list">
    <!-- Page Content goes here -->
    <table class="responsive-table">
        <thead>
            <tr>
                <th>School</th>
                <th>Address</th>
                <th>Sector</th>
                <th>Year Range</th>
                <th>Website</th>
                <th></th>
            </tr>
        </thead>
        <a href=""></a>
        <tbody>
            {{#each school}}
            <tr data-lat="{{dataValues.latitude}}" data-long="{{dataValues.longitude}}"
                data-teachersFTE="{{dataValues.teachingStaffFTE}}" data-enrolmentsFTE="{{dataValues.enrolmentsFTE}}"
                data-enrolments="{{dataValues.enrolmentsTotal}}">
                <td>{{dataValues.schoolName}}</td>
                <td>{{dataValues.suburb}}, {{dataValues.postcode}}</td>
                <td>{{dataValues.schoolSector}}, {{dataValues.schoolType}}</td>
                <td>{{dataValues.yearRange}}</td>
                <td><a href="{{dataValues.schoolURL}}" target="_blank">{{dataValues.schoolURL}}</a></td>
                <td id="btnIgnore"><a class="waves-effect waves-light btn deleteBtn" data-id="{{dataValues.id}}"><i
                            class="material-icons">delete_forever</i></a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>
<div class="container mapDiv">
    {{> map}}
</div>


{{> footer}}